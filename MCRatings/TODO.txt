
Settings:
- create missing fields?
- startup check: single popup for multiple missing fields

TMDB:
- get posters?
- get actor pics?

Main:
- Get all/row/column from OMDB/TMDB (override settings)
- locked fields (no overwrite/ignore changes - per movie)
- drop multiple collection files
- GET after drop
- use obsolete cache files when keys expired
